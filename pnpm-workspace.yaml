packages:
  - '.'

# This repository is a single-package Vite/Express application.
# Keeping the root package explicit prevents pnpm workspace parsing failures on Vercel.

catalog:
  # Intentionally empty; dependency versions remain in package.json.
